{
  "name": "project-viewer",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "main": "plugin.ts",
  "scripts": {
    "build": "pnpm -r build",
    "start": "node apps/server/dist/server.js",
    "dev:server": "pnpm --filter viewer-server run dev",
    "dev:client": "pnpm --filter viewer-client run dev",
    "dev": "pnpm -r --parallel run dev",
    "test:restart": "powershell -ExecutionPolicy Bypass -File scripts/test-nssm-restart.ps1"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
